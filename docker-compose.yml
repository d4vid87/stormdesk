services:
  stormdesk:
    image: ghcr.io/d4vid87/stormdesk:latest
    container_name: stormdesk
    restart: unless-stopped
    # Required on Linux: the hub's UDP broadcast never reaches a bridged network.
    network_mode: host
    volumes:
      - ./data:/data
